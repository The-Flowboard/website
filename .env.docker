# Local Docker development environment
# This file is committed to git — it contains no production secrets
# Docker mounts this as .env inside the container

APP_ENV=local

DB_HOST=mysql
DB_NAME=jmc_website
DB_USER=jmc_user
DB_PASS=local_dev_password

BLOG_API_KEY=dev_blog_api_key_local
N8N_API_KEY=dev_n8n_key_local
